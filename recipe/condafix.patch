diff --git a/setup.py b/setup.py
index 120e8aa..ecd3da7 100644
--- a/setup.py
+++ b/setup.py
@@ -22,6 +22,7 @@ from Cython.Build import cythonize
 
 ROOT = Path(__file__).parent
 WINDOWS = os.name == "nt"
+CONDA = os.environ.get("CONDA_BUILD", 0)
 
 
 def readfile(filename):
@@ -65,6 +66,10 @@ if WINDOWS:
         "-DGSL_DLL",
         "-DWIN32",
     ]
+elif CONDA:
+    extra_compile_args += [
+        "-O3",
+    ]
 else:
     extra_compile_args += [
         "-O3",
